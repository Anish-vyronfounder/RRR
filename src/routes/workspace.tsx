import { createFileRoute } from '@tanstack/react-router'
export const Route = createFileRoute('/workspace')({ component: () => <section className="rrr-card"><h1>Workspace</h1><p>RRR workspace.</p></section> })
