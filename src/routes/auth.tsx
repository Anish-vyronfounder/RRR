import { createFileRoute } from '@tanstack/react-router'
export const Route = createFileRoute('/auth')({ component: () => <section className="rrr-card"><h1>Account</h1><p>Authentication will be enabled before production monetization.</p></section> })
