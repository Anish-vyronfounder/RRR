import { createFileRoute } from '@tanstack/react-router'
export const Route = createFileRoute('/architecture')({ component: () => <section className="rrr-card"><h1>Architecture</h1><p>Vision → component data → compatibility → RAG → Repair / Reuse / Build.</p></section> })
