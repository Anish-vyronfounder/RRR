import { createFileRoute } from '@tanstack/react-router'; export const Route=createFileRoute('/build-with-what-i-have')({component:()=> <section className="rrr-card"><h1>Build With What I Have</h1><p>Add components and check compatible combinations.</p></section>})
